# Specs index

Lookup table for search tool. Add 10+ keyword variants per spec to improve hit rate.

## Format:
## <feature>.md
## Keywords: term1, term2, synonym1, synonym2, related1, related2, ...
## Description: one line

## kbuild-system.md
Keywords: kbuild, makefile, make, build system, linux kernel, kconfig, menuconfig, obj-y, recursive make, out-of-tree, O=, V=, verbosity, cross-compile, CROSS_COMPILE, dependency tracking, -MD, -MP, defconfig, allnoconfig, allyesconfig, mrproper, distclean, clean, cmake replacement, gnu make, build infrastructure, configuration menu, ncurses, auto.conf, autoconf.h, scripts/Makefile.build, scripts/Kbuild.include, conditional compilation, CONFIG_, test runner, benchmark build
Description: Replace CMake with Linux kernel-style Kbuild system using pure Makefiles and Kconfig
